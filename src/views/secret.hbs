<section data-post class="d-flex flex-row">
    {{#each kotiki}}

    <div data-id='${{this.sidFromUser1}}' class="d-flex bd-highlight mb-3">
        <div class="card" style="max-width: 400px;"><img src="{{this.link}}" class="img-thumbnail" alt="...">
            <h1>{{this.name}}</h1>
        </div>
        <div>
            <button data-id='${{this.sidFromUser1}}' data-action="delete" type="submit"
                class="btn btn-danger">Удалить</button>
        </div>
    </div>


    {{/each}}

</section>